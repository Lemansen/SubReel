<Window x:Class="SubReel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:SubReel"
        Title="SubReel Studio" Height="700" Width="1100" 
        Background="Transparent" WindowStartupLocation="CenterScreen"
        AllowsTransparency="True" WindowStyle="None"
        ResizeMode="CanMinimize"
        UseLayoutRounding="True" 
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        RenderOptions.BitmapScalingMode="HighQuality">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" 
                            GlassFrameThickness="0" 
                            CornerRadius="0" 
                            ResizeBorderThickness="0"
                            NonClientFrameEdges="None"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <LinearGradientBrush x:Key="PremiumProgress"
                     StartPoint="0,0"
                     EndPoint="1,0">
            <GradientStop Color="#4C85FF" Offset="0"/>
            <GradientStop Color="#78C8FF" Offset="1"/>
        </LinearGradientBrush>
        <local:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <LinearGradientBrush x:Key="ProgressShimmer" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#00FFFFFF" Offset="0"/>
            <GradientStop Color="#55FFFFFF" Offset="0.5"/>
            <GradientStop Color="#00FFFFFF" Offset="1"/>
        </LinearGradientBrush>

        <Storyboard x:Key="ProgressShimmerAnim" RepeatBehavior="Forever">
            <DoubleAnimation
        Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
        From="-200" To="400"
        Duration="0:0:1.2" />
        </Storyboard>
        <Storyboard x:Key="OnlinePulse" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetName="OnlineCircle"
                     Storyboard.TargetProperty="Opacity"
                     From="0.4"
                     To="1"
                     Duration="0:0:1.2"/>
        </Storyboard>
        <Storyboard x:Key="PulseUpdateAnim" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="UpdateBadge" Storyboard.TargetProperty="Opacity" From="1.0" To="0.5" Duration="0:0:1.5" AutoReverse="True"/>
            <DoubleAnimation Storyboard.TargetName="UpdateBadge" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="1.0" To="1.05" Duration="0:0:1.5" AutoReverse="True"/>
        </Storyboard>

        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.0" To="1.0" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="HeartbeatAnim" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="1.0" To="1.06" Duration="0:0:0.8" AutoReverse="True">
                <DoubleAnimation.EasingFunction>
                    <SineEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="1.0" To="1.06" Duration="0:0:0.8" AutoReverse="True">
                <DoubleAnimation.EasingFunction>
                    <SineEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="FadeInPanel">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <QuarticEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="FadeInAuth">
            <DoubleAnimation Storyboard.TargetName="AuthOverlay" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetName="AuthChild" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="0.8" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <PowerEase Power="5" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="AuthChild" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="0.8" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <PowerEase Power="5" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="FadeOutAuth">
            <DoubleAnimation Storyboard.TargetName="AuthOverlay" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="AuthChild" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="1" To="0.8" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <PowerEase Power="5" EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="AuthChild" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="1" To="0.8" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <PowerEase Power="5" EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

            <Grid x:Name="MainRoot">
        <Canvas x:Name="NotificationCanvas"
        IsHitTestVisible="False"
        Grid.ColumnSpan="2"
        Panel.ZIndex="999">

            <!-- ===== УВЕДОМЛЕНИЕ 1 ===== -->
            <Border x:Name="NotificationToast"
            Width="320"
            MinHeight="65"
            Padding="0,15,0,15"
            Background="{StaticResource CardBg}"
            CornerRadius="16"
            BorderThickness="2"
            BorderBrush="{StaticResource AccentBlue}"
            Canvas.Left="400"
            Canvas.Top="-100"
            Opacity="0"
            RenderTransformOrigin="0.5,0.5">

                <Border.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Border.RenderTransform>

                <Border.Effect>
                    <DropShadowEffect Color="{Binding Color, Source={StaticResource AccentBlue}}"
                              BlurRadius="10"
                              Opacity="0.5"
                              ShadowDepth="0"/>
                </Border.Effect>

                <Grid Margin="15,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Ellipse Width="10"
                     Height="10"
                     Fill="{StaticResource AccentBlue}"
                     VerticalAlignment="Center"
                     Margin="0,0,15,0">
                        <Ellipse.Effect>
                            <BlurEffect Radius="4"/>
                        </Ellipse.Effect>
                    </Ellipse>

                    <TextBlock x:Name="NotificationText"
                       Grid.Column="1"
                       Text="Уведомление"
                       Foreground="White"
                       FontSize="13"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"/>
                </Grid>
            </Border>

            <!-- ===== УВЕДОМЛЕНИЕ 2 ===== -->
            <Border x:Name="NotificationToast2"
            Width="320"
            MinHeight="65"
            Padding="0,15,0,15"
            Background="{StaticResource CardBg}"
            CornerRadius="16"
            BorderThickness="2"
            BorderBrush="{StaticResource AccentBlue}"
            Canvas.Left="400"
            Canvas.Top="-100"
            Opacity="0"
            RenderTransformOrigin="0.5,0.5">

                <Border.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Border.RenderTransform>

                <Border.Effect>
                    <DropShadowEffect Color="{Binding Color, Source={StaticResource AccentBlue}}"
                              BlurRadius="10"
                              Opacity="0.5"
                              ShadowDepth="0"/>
                </Border.Effect>

                <Grid Margin="15,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Ellipse Width="10"
                     Height="10"
                     Fill="{StaticResource AccentBlue}"
                     VerticalAlignment="Center"
                     Margin="0,0,15,0">
                        <Ellipse.Effect>
                            <BlurEffect Radius="4"/>
                        </Ellipse.Effect>
                    </Ellipse>

                    <TextBlock x:Name="NotificationText2"
                       Grid.Column="1"
                       Text="Уведомление"
                       Foreground="White"
                       FontSize="13"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"/>
                </Grid>
            </Border>

        </Canvas>
        <Border x:Name="MainBackground" 
        CornerRadius="20" 
        Background="{StaticResource BgDark}" 
        BorderThickness="2" 
        BorderBrush="{StaticResource BorderSharp}" 
        MouseLeftButtonDown="MainBackground_MouseLeftButtonDown"
        HorizontalAlignment="Stretch" 
        VerticalAlignment="Stretch">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="#0A0A0E" CornerRadius="20,0,0,20" BorderThickness="0,0,2,0" BorderBrush="{StaticResource BorderSharp}">
                    <DockPanel Margin="0,40,0,25">
                        <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Center">
                            <Grid x:Name="ProfileRegion" Cursor="Hand" MouseLeftButtonDown="ProfileRegion_Click" Background="Transparent" Margin="0,0,0,30">
                                <StackPanel>
                                    <Grid Width="75" Height="75" HorizontalAlignment="Center">
                                        <Ellipse Fill="#14151B" Stroke="{StaticResource AccentBlue}" StrokeThickness="2.5">
                                            <Ellipse.Effect>
                                                <DropShadowEffect Color="#3374FF" BlurRadius="20" ShadowDepth="0" Opacity="0.4" RenderingBias="Performance"/>
                                            </Ellipse.Effect>
                                        </Ellipse>
                                        <Border Width="48" Height="48" CornerRadius="8" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Border.Background>
                                                <ImageBrush x:Name="UserAvatarImg" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                                            </Border.Background>
                                        </Border>
                                    </Grid>
                                    <TextBlock x:Name="DisplayNick" Text="Player" Foreground="White" FontSize="16" FontWeight="Black" HorizontalAlignment="Center" Margin="0,15,0,0"/>

                                    <Border x:Name="AccountTypeBadge" Background="#1AFFFFFF" CornerRadius="6" HorizontalAlignment="Center" Margin="0,8,0,0" Padding="12,4">
                                        <TextBlock x:Name="AccountTypeStatus" Text="OFFLINE" Foreground="#FFA500" FontSize="10" VerticalAlignment="Center" FontWeight="Bold"/>
                                    </Border>
                                    <TextBlock x:Name="AppVersionText" Text="v0.1.2" Foreground="#444" FontSize="9" FontWeight="Black" HorizontalAlignment="Center" Margin="0,6,0,0" Opacity="0.6" Cursor="Hand" MouseLeftButtonDown="AppVersionText_Click"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                        <Border Name="UpdateBadge" DockPanel.Dock="Bottom" Visibility="Collapsed" Background="#FFCC00" CornerRadius="12" Padding="15,10" HorizontalAlignment="Center" Margin="0,20,0,0" Cursor="Hand" MouseLeftButtonDown="UpdateBadge_Click" RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect Color="#FFCC00" BlurRadius="15" ShadowDepth="0" Opacity="0.5" RenderingBias="Performance"/>
                            </Border.Effect>
                            <TextBlock Name="LauncherVersionText" Text="ОБНОВЛЕНИЕ" FontSize="11" FontWeight="Black" Foreground="Black" IsHitTestVisible="False"/>
                        </Border>
                        <StackPanel VerticalAlignment="Top">
                            <StackPanel Width="190" HorizontalAlignment="Center">
                                <Button x:Name="BtnBuilds" Content="СБОРКИ" Style="{StaticResource SidebarButtonStyle}" Click="BackToBuilds_Click" Tag="Active"/>
                                <Button x:Name="BtnCommunity" Content="СООБЩЕСТВО" Style="{StaticResource SidebarButtonStyle}" Click="CommunityBtn_Click"/>
                                <Button x:Name="BtnNews" Content="НОВОСТИ" Style="{StaticResource SidebarButtonStyle}" Click="NewsBtn_Click"/>
                                <Button x:Name="BtnSettings" Content="НАСТРОЙКИ" Style="{StaticResource SidebarButtonStyle}" Click="SettingsBtn_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </DockPanel>
                </Border>
                <Grid Grid.Column="1" Margin="35">
                    <TextBlock Text="SubReel Studio" Foreground="White" FontSize="26" FontWeight="Black" Margin="0,0,0,30">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="12" Opacity="0.5" ShadowDepth="3" RenderingBias="Performance"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <UniformGrid x:Name="BuildsPanel" Columns="2" VerticalAlignment="Top" Margin="0,60,0,0">
                        <UniformGrid.RenderTransform>
                            <TranslateTransform/>
                        </UniformGrid.RenderTransform>
                        <Button x:Name="BtnVanilla" Tag="Selected" Content="1.21.1" Click="SelectVersion_Click" PreviewMouseRightButtonDown="BtnVanilla_RightClick" Height="160" Margin="10">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Grid Background="Transparent" Cursor="Hand">
                                        <Border x:Name="Shadow" Background="{StaticResource AccentBlue}" Opacity="0" CornerRadius="22" Margin="2">
                                            <Border.Effect>
                                                <BlurEffect Radius="20"/>
                                            </Border.Effect>
                                        </Border>
                                        <Border x:Name="Card" Background="{StaticResource CardBg}" CornerRadius="22" BorderThickness="2" BorderBrush="{StaticResource BorderSharp}" Margin="5" RenderTransformOrigin="0.5,0.5" CacheMode="BitmapCache">
                                            <Border.RenderTransform>
                                                <TranslateTransform Y="0"/>
                                            </Border.RenderTransform>
                                            <Grid>
                                                <Border x:Name="StatusBadge" Width="30" Height="30" Background="{StaticResource AccentBlue}" CornerRadius="8" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20" Opacity="0">
                                                    <TextBlock Text="✓" Foreground="White" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                </Border>
                                                <Border x:Name="VersionEditBtn" Width="36" Height="36" Background="#1AFFFFFF" CornerRadius="10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,15,15" Cursor="Hand" ToolTip="Сменить версию" PreviewMouseLeftButtonDown="VersionEditBtn_Click">
                                                    <TextBlock x:Name="EditIcon" Text="⚙" Foreground="White" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.6"/>
                                                </Border>
                                                <StackPanel VerticalAlignment="Bottom" Margin="25">
                                                    <TextBlock Text="Minecraft Vanilla" Foreground="White" FontWeight="Bold" FontSize="18" IsHitTestVisible="False"/>
                                                    <StackPanel Orientation="Horizontal" IsHitTestVisible="False" Margin="0,5,0,0">
                                                        <Border x:Name="StateBadge" Background="#1AFFFFFF" CornerRadius="5" Padding="7,2" Margin="0,0,8,0">
                                                            <TextBlock x:Name="StateText" Text="ВЫБРАТЬ ВЕРСИЮ" Foreground="#999" FontSize="9" FontWeight="Black"/>
                                                        </Border>
                                                        <TextBlock Text="{TemplateBinding Content}" Foreground="#999" FontSize="11" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="Tag" Value="Selected">
                                            <Setter TargetName="Card" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                                            <Setter TargetName="StatusBadge" Property="Opacity" Value="1"/>
                                            <Setter TargetName="StateBadge" Property="Background" Value="{StaticResource AccentBlue}"/>
                                            <Setter TargetName="StateText" Property="Text" Value="ВЫБРАНО"/>
                                            <Setter TargetName="StateText" Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger SourceName="VersionEditBtn" Property="IsMouseOver" Value="True">
                                            <Setter TargetName="VersionEditBtn" Property="Background" Value="#4C85FF"/>
                                            <Setter TargetName="EditIcon" Property="Opacity" Value="1"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="Card" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="-5" Duration="0:0:0.2"/>
                                                        <DoubleAnimation Storyboard.TargetName="Shadow" Storyboard.TargetProperty="Opacity" To="0.3" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="Card" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.2"/>
                                                        <DoubleAnimation Storyboard.TargetName="Shadow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="BtnCreateCustom" Click="CreateCustomVersion_Click" Height="160" Margin="10">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Grid Background="Transparent" Cursor="Hand">
                                        <Border x:Name="Shadow" Background="{StaticResource AccentBlue}" Opacity="0" CornerRadius="22" Margin="2">
                                            <Border.Effect>
                                                <BlurEffect Radius="20"/>
                                            </Border.Effect>
                                        </Border>
                                        <Border x:Name="Card" Background="{StaticResource CardBg}" CornerRadius="22" BorderThickness="2" BorderBrush="{StaticResource BorderSharp}" Margin="5" CacheMode="BitmapCache">
                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <TextBlock Text="+" Foreground="{StaticResource AccentBlue}" FontSize="48" FontWeight="Light" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                                <TextBlock Text="СОЗДАТЬ СБОРКУ" Foreground="White" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center"/>
                                                <TextBlock Text="Свои моды и настройки" Foreground="#666" FontSize="10" Margin="0,5,0,0" HorizontalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Card" Property="Background" Value="#181921"/>
                                            <Setter TargetName="Card" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                                            <Setter TargetName="Shadow" Property="Opacity" Value="0.2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </UniformGrid>
                    <Grid x:Name="NewsPanel" Visibility="Collapsed" Opacity="0" Margin="0,60,0,85">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.RenderTransform>
                            <TranslateTransform Y="20"/>
                        </Grid.RenderTransform>
                        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="0,0,15,0" CanContentScroll="True">
                            <ItemsControl x:Name="NewsItemsControl" VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel IsVirtualizing="True" VirtualizationMode="Recycling"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,30" Background="#111217" CornerRadius="20" BorderThickness="1" BorderBrush="#1C1D26">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="8" Opacity="0.1" ShadowDepth="1" Color="Black"/>
                                            </Border.Effect>
                                            <Grid>
                                                <Border Width="4" Background="{Binding AccentBrush}" HorizontalAlignment="Left" VerticalAlignment="Stretch" Margin="0,30,0,30"
        CornerRadius="0,20,20,0"/>
                                                <StackPanel Margin="35,30,35,30">
                                                    <Grid Margin="0,0,0,15">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <Border Background="#0AFFFFFF" CornerRadius="6" Padding="10,5" BorderThickness="1" BorderBrush="{Binding AccentBrush}">
                                                            <TextBlock Text="{Binding Category}" Foreground="{Binding AccentBrush}" FontSize="10" FontWeight="Black"/>
                                                        </Border>
                                                        <TextBlock Grid.Column="1" Text="{Binding Date}" Foreground="#777" FontSize="11" Margin="15,0,0,0" VerticalAlignment="Center" FontWeight="Bold"/>
                                                    </Grid>
                                                    <TextBlock Text="{Binding Title}" Foreground="White" FontSize="26" FontWeight="Black" TextWrapping="Wrap" Margin="0,0,0,12"/>
                                                    <TextBlock Text="{Binding Description}" Foreground="#A1A1A8" FontSize="14" LineHeight="22" TextWrapping="Wrap" Margin="0,0,0,25"/>
                                                    <Border Background="#08080A" CornerRadius="16" BorderThickness="1" BorderBrush="#1A1B23" Padding="25,20">
                                                        <ItemsControl ItemsSource="{Binding Changes}">
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <Grid Margin="0,6">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="20"/>
                                                                            <ColumnDefinition Width="*"/>
                                                                        </Grid.ColumnDefinitions>
                                                                        <Ellipse Width="6" Height="6" Fill="{StaticResource AccentBlue}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,7,0,0" Opacity="0.9"/>
                                                                        <TextBlock Grid.Column="1" Text="{Binding}" Foreground="#CCCCCC" FontSize="13" LineHeight="20" TextWrapping="Wrap"/>
                                                                    </Grid>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </Border>
                                                    <Button Content="{Binding ButtonText}" Tag="{Binding ButtonUrl}" Visibility="{Binding ButtonVisibility}" Click="OpenNewsUrl_Click" Margin="0,25,0,0" HorizontalAlignment="Left" Cursor="Hand">
                                                        <Button.Template>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border x:Name="BtnBg" Background="#0CFFFFFF" BorderThickness="1" BorderBrush="#22FFFFFF" CornerRadius="12" Padding="20,12">
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <TextBlock x:Name="t" Text="{TemplateBinding Content}" Foreground="#E0E0E0" FontSize="11" FontWeight="Black"/>
                                                                        <TextBlock x:Name="icon" Text="➔" Foreground="{StaticResource AccentBlue}" Margin="10,0,0,0" FontSize="11" FontWeight="Black" VerticalAlignment="Center"/>
                                                                    </StackPanel>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="BtnBg" Property="Background" Value="#1AFFFFFF"/>
                                                                        <Setter TargetName="t" Property="Foreground" Value="White"/>
                                                                        <Setter TargetName="icon" Property="Foreground" Value="White"/>
                                                                    </Trigger>
                                                                    <Trigger Property="IsPressed" Value="True">
                                                                        <Setter TargetName="BtnBg" Property="Background" Value="{StaticResource AccentBlue}"/>
                                                                        <Setter TargetName="BtnBg" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Button.Template>
                                                    </Button>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                    <Grid x:Name="CommunityPanel" Visibility="Collapsed" Margin="0,60,0,85" CacheMode="BitmapCache">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="#0C0D12" CornerRadius="18" BorderThickness="1" BorderBrush="{StaticResource BorderSharp}" Margin="0,0,15,0">
                            <Grid Margin="15">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Width="32" Height="32" CornerRadius="6" VerticalAlignment="Top" Margin="0,2,10,0">
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding AvatarUrl}" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                                    </Border.Background>
                                </Border>
                                <TextBlock Text="ДРУЗЬЯ В СЕТИ" Foreground="{StaticResource AccentBlue}" FontSize="10" FontWeight="Black" Margin="5,0,0,12"/>
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                    <StackPanel x:Name="FriendsList">
                                        <Border Background="{StaticResource CardBg}" CornerRadius="10" Padding="8" Margin="0,0,0,6">
                                            <StackPanel Orientation="Horizontal">
                                                <Ellipse Width="10" Height="10" Fill="#20F289" Margin="5,0,10,0"/>
                                                <TextBlock Text="Online_Player" Foreground="White" FontSize="12" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                        <Border Grid.Column="1" Background="#0C0D12" CornerRadius="18" BorderThickness="1" BorderBrush="{StaticResource BorderSharp}">
                            <Grid Margin="15">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <ScrollViewer x:Name="ChatScroll" Margin="0,0,0,10">
                                    <ItemsControl x:Name="ChatMessages">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Margin="0,4">
                                                    <TextBlock Text="{Binding Nickname}" Foreground="{StaticResource AccentBlue}" FontSize="11" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Message}" Foreground="White" FontSize="13" TextWrapping="Wrap"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                                <Border Grid.Row="1" Background="{StaticResource BgDark}" CornerRadius="12" BorderThickness="1" BorderBrush="{StaticResource BorderSharp}" Padding="5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="ChatMessageInput" Background="Transparent" BorderThickness="0" Foreground="White" Padding="10,6" VerticalContentAlignment="Center" KeyDown="ChatMessageInput_KeyDown" CaretBrush="{StaticResource AccentBlue}"/>
                                        <Button Grid.Column="1" Content="➔" Foreground="{StaticResource AccentBlue}" Background="Transparent" BorderThickness="0" FontWeight="Bold" FontSize="18" Width="45" Click="SendMessage_Click" Cursor="Hand"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>
                    <Grid x:Name="SettingsPanel"
      Visibility="Collapsed"
      Opacity="0"
      Margin="0,50,0,90"
      CacheMode="BitmapCache">

                        <Grid.RenderTransform>
                            <TranslateTransform Y="0"/>
                        </Grid.RenderTransform>

                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                  Padding="0,0,15,0">

                            <StackPanel Margin="10,0">

                                <!-- ================= SYSTEM ================= -->
                                <Border Background="#12131A"
                    CornerRadius="18"
                    Padding="20"
                    Margin="0,0,0,12"
                    BorderThickness="1"
                    BorderBrush="#1F2026">

                                    <StackPanel>

                                        <Grid Margin="0,0,0,20">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="[ SYSTEM ]"
                                       Foreground="{StaticResource AccentBlue}"
                                       FontWeight="Black"
                                       FontSize="10"
                                       Margin="0,0,15,0"
                                       Opacity="0.5"/>
                                                <TextBlock Text="ОСНОВНЫЕ ПАРАМЕТРЫ"
                                       Foreground="White"
                                       FontWeight="Bold"
                                       FontSize="12"/>
                                            </StackPanel>

                                            <Button x:Name="KillGameBtn"
                                Content="ЗАВЕРШИТЬ ПРОЦЕСС ИГРЫ"
                                Style="{StaticResource KillButtonStyle}"
                                HorizontalAlignment="Right"
                                Click="KillGame_Click">
                                                <Button.ToolTip>
                                                    <ToolTip Content="Игра не запущена"
                                         BorderBrush="#FF4444"/>
                                                </Button.ToolTip>
                                            </Button>
                                        </Grid>

                                        <!-- Java block -->
                                        <Border Background="#0D0E14"
                            CornerRadius="10"
                            Padding="12"
                            Margin="0,0,0,15"
                            BorderBrush="#1C1D26"
                            BorderThickness="1">

                                            <StackPanel>

                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <CheckBox x:Name="JavaCheck"
                                          Content="Автопоиск Java"
                                          IsChecked="True"
                                          Margin="0,0,20,0"/>

                                                    <Button Content="Указать Java"
                                        Click="SelectJava_Click"
                                        Style="{StaticResource SecondaryButton}"
                                        IsEnabled="{Binding IsChecked, ElementName=JavaCheck, Converter={StaticResource InverseBoolConverter}}"/>

                                                    <Button x:Name="ReinstallJavaBtn"
        Content="ПЕРЕУСТАНОВИТЬ JAVA"
        Margin="8,0,0,0"
        Click="ReinstallJava_Click"
        Style="{StaticResource SecondaryButton}"/>

                                                    <CheckBox x:Name="ConsoleCheck"
                                          Content="Консоль при запуске"
                                          Margin="15,0,0,0"
                                          Click="ConsoleCheck_Click"/>
                                                </StackPanel>

                                                <TextBlock x:Name="JavaSourceBox"
                                       Text="Java: auto"
                                       Foreground="#6FA8FF"
                                       FontSize="10"
                                       Margin="2,6,0,0"
                                       Opacity="0.8"/>

                                            </StackPanel>

                                        </Border>

                                        <!-- Paths -->
                                        <StackPanel>

                                            <Button Click="OpenFolder_Click"
                                HorizontalAlignment="Left"
                                Cursor="Hand">

                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="btn"
                                            Background="Transparent"
                                            Padding="0,8"
                                            BorderThickness="0,0,0,1"
                                            BorderBrush="#1AFFFFFF">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="DIR"
                                                       Foreground="{StaticResource AccentBlue}"
                                                       FontWeight="Black"
                                                       FontSize="9"
                                                       Margin="0,0,15,0"/>
                                                                <TextBlock Text="ОТКРЫТЬ КОРНЕВУЮ ПАПКУ ИГРЫ"
                                                       Foreground="#888"
                                                       FontSize="10"
                                                       FontWeight="Bold"/>
                                                            </StackPanel>
                                                        </Border>

                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="btn"
                                                    Property="BorderBrush"
                                                    Value="{StaticResource AccentBlue}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>

                                            </Button>

                                            <Button Click="OpenLogsFolder_Click"
                                Margin="0,5,0,0"
                                HorizontalAlignment="Left"
                                Cursor="Hand">

                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="btn"
                                            Background="Transparent"
                                            Padding="0,8"
                                            BorderThickness="0,0,0,1"
                                            BorderBrush="#1AFFFFFF">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="LOGS"
                                                       Foreground="#FFCC00"
                                                       FontWeight="Black"
                                                       FontSize="9"
                                                       Margin="0,0,15,0"/>
                                                                <TextBlock Text="ПОСМОТРЕТЬ ЖУРНАЛ СОБЫТИЙ"
                                                       Foreground="#888"
                                                       FontSize="10"
                                                       FontWeight="Bold"/>
                                                            </StackPanel>
                                                        </Border>

                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="btn"
                                                    Property="BorderBrush"
                                                    Value="#FFCC00"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>

                                            </Button>

                                        </StackPanel>

                                    </StackPanel>
                                </Border>

                                <!-- ================= RAM ================= -->
                                <Border Background="#12131A"
                    CornerRadius="18"
                    Padding="20"
                    Margin="0,0,0,12"
                    BorderThickness="1"
                    BorderBrush="#1F2026">

                                    <StackPanel>

                                        <Grid Margin="0,0,0,20">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="[ RAM ]"
                                       Foreground="{StaticResource AccentBlue}"
                                       FontWeight="Black"
                                       FontSize="10"
                                       Margin="0,0,15,0"
                                       Opacity="0.5"/>
                                                <TextBlock Text="ВЫДЕЛЕНИЕ ПАМЯТИ"
                                       Foreground="White"
                                       FontWeight="Bold"
                                       FontSize="12"/>
                                            </StackPanel>

                                            <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                                                <TextBlock x:Name="GbText"
                                       Text="4.0 GB"
                                       Foreground="{StaticResource AccentBlue}"
                                       FontWeight="Bold"
                                       FontSize="11"
                                       Margin="0,0,10,0"/>

                                                <Border Background="#0B0C12"
            CornerRadius="8"
            Padding="8,3"
            BorderThickness="1"
            BorderBrush="#2A2D3A">
                                                    <TextBox x:Name="RamInput"
                                         Text="4096"
                                         Foreground="White"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Width="60"
                                         TextAlignment="Center"
                                         CaretBrush="{StaticResource AccentBlue}"                    
                                         TextChanged="RamInput_TextChanged"/>
                                                </Border>
                                            </StackPanel>
                                        </Grid>

                                        <Slider x:Name="RamSlider"
                            Minimum="1024"
                            Maximum="16384"
                            Value="4096"
                            TickFrequency="512"
                            IsSnapToTickEnabled="True"
                            ValueChanged="RamSlider_ValueChanged"
                            Style="{StaticResource ModernSliderStyle}"/>

                                    </StackPanel>
                                </Border>

                                <!-- ================= TERMINAL ================= -->
                                <Border Background="#0A0B10"
                    CornerRadius="18"
                    Padding="15"
                    BorderThickness="1"
                    BorderBrush="#1A1B23">

                                    <StackPanel>

                                        <Grid Margin="5,0,5,10">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="TERMINAL"
                                       Foreground="#444"
                                       FontWeight="Black"
                                       FontSize="10"
                                       Margin="0,0,12,0"/>

                                                <Button Content="COPY"
        Click="CopyLogs_Click"
        Style="{StaticResource SecondaryButton}"/>
                                            </StackPanel>

                                            <Button Content="CLEAR"
                                HorizontalAlignment="Right"
                                Click="ClearLogs_Click"
                                                    Style="{StaticResource SecondaryButton}"/>
                                        </Grid>

                                        <Border Background="#040406"
                            CornerRadius="12"
                            BorderThickness="1"
                            BorderBrush="#111">

                                            <ScrollViewer x:Name="LogScroll"
                                      Margin="10"
                                      Height="120"
                                      VerticalScrollBarVisibility="Auto">

                                                <TextBlock x:Name="LogText"
                                       Text="[SYSTEM] Waiting for command..."
                                       Foreground="#444"
                                       FontSize="10"
                                       FontFamily="Consolas"
                                       TextWrapping="Wrap"/>
                                            </ScrollViewer>
                                        </Border>

                                    </StackPanel>
                                </Border>

                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                    <Grid VerticalAlignment="Bottom" Panel.ZIndex="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="190"/>
                        </Grid.ColumnDefinitions>

                        <!-- ================= ЛЕВАЯ ЧАСТЬ ================= -->
                        <Grid Margin="0,0,25,0" VerticalAlignment="Bottom">

                            <Grid.RowDefinitions>
                                <RowDefinition Height="24"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock x:Name="StatusLabel"
                       Text="Готов к запуску"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>

                                <TextBlock x:Name="ConsoleIndicator"
                       Grid.Column="1"
                       Text="CONSOLE ON"
                       Foreground="#78C8FF"
                       FontSize="11"
                       Margin="10,0,0,0"
                       Visibility="Collapsed"
                       VerticalAlignment="Center"/>

                                <TextBlock x:Name="SelectedVersionBottom"
                       Grid.Column="2"
                       Text="Vanilla 1.21.1"
                       Foreground="#999"
                       FontSize="11"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="20,0,0,0"
                        VerticalAlignment="Center">

                                    <Ellipse x:Name="OnlineCircle"
                         Width="8"
                         Height="8"
                         Fill="Gray"
                         Margin="0,0,6,0"/>

                                    <TextBlock x:Name="OnlineCount"
                           Text="OFFLINE"
                           Foreground="#AAA"
                           FontSize="11"/>
                                </StackPanel>

                                <Button x:Name="CancelDownloadBtn"
                    Grid.Column="4"
                    Content="ОТМЕНИТЬ"
                    Margin="15,0,0,0"
                    VerticalAlignment="Center"
                    Visibility="Collapsed"
                    Click="CancelDownload_Click"/>
                            </Grid>

                        </Grid>

                        <!-- ================= ПРАВАЯ ЧАСТЬ ================= -->
                        <Grid Grid.Column="1">
                            <Border Background="{StaticResource AccentBlue}"
                CornerRadius="18"
                Opacity="0.4">
                                <Border.Effect>
                                    <BlurEffect Radius="25"/>
                                </Border.Effect>
                            </Border>

                            <Button x:Name="PlayBtn"
        Content="ИГРАТЬ"
        Click="Play_Click"
        Style="{StaticResource AnimatedAccentPlayButtonStyle}"/>

                        </Grid>
                    </Grid>
                    <Grid x:Name="AuthOverlay" Visibility="Collapsed" Grid.ColumnSpan="2" CacheMode="BitmapCache">
                    <Border Background="#000000" Opacity="0.85" MouseLeftButtonDown="AuthOverlay_Close"/>
                    <Border x:Name="AuthChild" Width="340" VerticalAlignment="Center" HorizontalAlignment="Center" Background="{StaticResource CardBg}" CornerRadius="20" BorderThickness="2" BorderBrush="{StaticResource AccentBlue}" RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </Border.RenderTransform>
                        <StackPanel Margin="30">
                            <TextBlock Text="АВТОРИЗАЦИЯ" Foreground="White" FontWeight="Black" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,25"/>

                            <TextBlock Text="Ваш никнейм" Foreground="{StaticResource TextGray}" FontSize="12" FontWeight="Bold" Margin="5,0,0,8"/>

                            <TextBox x:Name="NicknameBox" Text="Player" Background="#08080A" Foreground="White" BorderThickness="1.5" BorderBrush="{StaticResource BorderSharp}" Padding="12" FontSize="16" FontWeight="Bold" CaretBrush="{StaticResource AccentBlue}" MaxLength="16" PreviewTextInput="NicknameBox_PreviewTextInput" TextChanged="NicknameBox_TextChanged">
                                <TextBox.Resources>
                                    <Style TargetType="{x:Type Border}">
                                        <Setter Property="CornerRadius" Value="10"/>
                                    </Style>
                                </TextBox.Resources>
                            </TextBox>
                            <Button Content="ПРИМЕНИТЬ" Click="SaveAuth_Click" Margin="0,20,0,0" Height="50" Background="{StaticResource AccentBlue}" Foreground="White" FontWeight="Black" FontSize="14">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="15">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="BtnBorder" Property="Background" Value="#3374FF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <TextBlock Text="или" Foreground="{StaticResource TextGray}" HorizontalAlignment="Center" Margin="0,15,0,15" FontSize="12"/>

                            <Button x:Name="MSLoginBtn" Content="ВОЙТИ ЧЕРЕЗ MICROSOFT" Click="MSLogin_Click" Height="45" Background="#2D2D2D" Foreground="White" FontWeight="Bold" FontSize="12">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="MSBorder" Background="{TemplateBinding Background}" CornerRadius="12" BorderThickness="1" BorderBrush="#444">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="MSBorder" Property="Background" Value="#3D3D3D"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </Border>
                </Grid>
                <Grid x:Name="VersionOverlay" Visibility="Collapsed" Grid.ColumnSpan="2">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="IsHitTestVisible" Value="True"/>
                            <Style.Triggers>
                                <Trigger Property="Visibility" Value="Collapsed">
                                    <Setter Property="IsHitTestVisible" Value="False"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Border Background="#000000" Opacity="0.8" MouseLeftButtonDown="CloseVersionOverlay_Click"/>
                    <Border Width="380" VerticalAlignment="Center" HorizontalAlignment="Center" Background="{StaticResource CardBg}" CornerRadius="25" BorderThickness="2" BorderBrush="{StaticResource AccentBlue}">
                        <StackPanel Margin="30">
                            <TextBlock Text="ВЫБОР ВЕРСИИ" Foreground="White" FontWeight="Black" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Выберите версию игры для запуска"
           Foreground="{StaticResource TextGray}"
           FontSize="12"
           HorizontalAlignment="Center"
           Margin="0,0,0,10"
           TextWrapping="Wrap"
           TextAlignment="Center"/>

                            <CheckBox x:Name="ShowSnapshotsCheckBox"
          Content="Дополнительные версии"
          Foreground="White"
          HorizontalAlignment="Center"
          Margin="0,0,0,15"
          Checked="ShowSnapshotsCheckBox_Changed"
          Unchecked="ShowSnapshotsCheckBox_Changed"/>

                            <Border Background="#08080A" CornerRadius="15" BorderThickness="1" BorderBrush="{StaticResource BorderSharp}">
                                <ListBox x:Name="VersionListBox"
             Background="Transparent"
             BorderThickness="0"
             Foreground="White"
             FontSize="14"
             Padding="5"
             Height="200">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="10" Padding="15,10" Margin="2">
                                                            <ContentPresenter />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="ItemBorder" Property="Background" Value="#1A1B23"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource AccentBlue}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                </ListBox>
                            </Border>
                            <Button Content="ПРИМЕНИТЬ" Click="ApplyVersion_Click" Margin="0,25,0,0" Height="50" Background="{StaticResource AccentBlue}" Foreground="White" FontWeight="Black" FontSize="14">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="15">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
            </Grid>
        </Border>
        <Grid x:Name="WelcomeOverlay" Visibility="Collapsed" Grid.ColumnSpan="2" Panel.ZIndex="1000" Opacity="0">
            <Border Background="#000000" Opacity="0.85" MouseLeftButtonDown="CloseWelcome_Click"/>
            <Border x:Name="WelcomeChild" Width="420" VerticalAlignment="Center" HorizontalAlignment="Center" Background="{StaticResource CardBg}" CornerRadius="25" BorderThickness="2" BorderBrush="{StaticResource AccentBlue}" RenderTransformOrigin="0.5,0.5">
                <Border.RenderTransform>
                    <ScaleTransform ScaleX="0.9" ScaleY="0.9"/>
                </Border.RenderTransform>
                <StackPanel Margin="35">
                    <Grid HorizontalAlignment="Center" Margin="0,0,0,25">
                        <Ellipse Width="70" Height="70" Fill="{StaticResource AccentBlue}" Opacity="0.15"/>
                        <TextBlock Text="!" Foreground="{StaticResource AccentBlue}" FontSize="34" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                    <TextBlock x:Name="WelcomeTitle" Text="" Foreground="White" FontWeight="Black" FontSize="22" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,12"/>
                    <TextBlock x:Name="WelcomeMessage" Text="" Foreground="{StaticResource TextGray}" FontSize="14" TextWrapping="Wrap" TextAlignment="Center" LineHeight="20" Margin="0,0,0,22"/>
                    <Border Background="#1AFFFFFF" CornerRadius="14" Padding="18" Margin="0,0,0,28">
                        <Grid HorizontalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="⚙" Foreground="{StaticResource AccentBlue}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="WelcomeSubMessage" Grid.Column="1" Text="" Foreground="White" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </Grid>
                    </Border>
                    <Button Content="ПОЕХАЛИ!" Click="CloseWelcome_Click" Height="52" Background="{StaticResource AccentBlue}" Foreground="White" FontWeight="Black" FontSize="15" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="BtnBorder"  Background="{TemplateBinding Background}" CornerRadius="16">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="BtnBorder" Property="Background" Value="#4C85FF"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="BtnBorder" Property="Opacity" Value="0.85"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
        <Grid x:Name="CrashOverlay"
      Visibility="Collapsed"
      Grid.ColumnSpan="2"
      Panel.ZIndex="2000"
      Opacity="0">

            <Border Background="#000000" Opacity="0.85"/>

            <Border Width="460"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Background="{StaticResource CardBg}"
            CornerRadius="25"
            BorderThickness="2"
            BorderBrush="#FF4444">

                <StackPanel Margin="30">

                    <TextBlock Text="ОШИБКА ЗАПУСКА"
                       Foreground="#FF4444"
                       FontWeight="Black"
                       FontSize="20"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,15"/>

                    <TextBlock x:Name="CrashReasonText"
                       Text="Причина"
                       Foreground="White"
                       FontSize="14"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Margin="0,0,0,20"/>

                    <Border Background="#08080A"
                    CornerRadius="12"
                    Padding="15"
                    BorderThickness="1"
                    BorderBrush="#1A1B23"
                    Margin="0,0,0,20">

                        <TextBlock x:Name="CrashHelpText"
                           Text="Рекомендации"
                           Foreground="#AAA"
                           FontSize="12"
                           TextWrapping="Wrap"/>
                    </Border>

                    <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center">

                        <Button Content="ОТКРЫТЬ ЛОГИ"
                        Width="150"
                        Margin="5"
                        Click="OpenLogsFolder_Click"/>

                        <Button Content="ЗАКРЫТЬ"
                        Width="120"
                        Margin="5"
                        Click="CloseCrashOverlay_Click"/>
                    </StackPanel>

                </StackPanel>
                
            </Border>
        </Grid>
        <StackPanel Orientation="Horizontal" 
            HorizontalAlignment="Right" 
            VerticalAlignment="Top" 
            Margin="0,10,10,0" 
            Panel.ZIndex="9999">
            <Button Content="─" 
            shell:WindowChrome.IsHitTestVisibleInChrome="True"
            Click="MinimizeBtn_Click" 
            Style="{StaticResource ControlButtonStyle}"/>
            <Button x:Name="MaximizeBtn" 
            Content="▢" 
            shell:WindowChrome.IsHitTestVisibleInChrome="True"
            Click="MaximizeBtn_Click" 
            Style="{StaticResource ControlButtonStyle}"/>
            <Button x:Name="CloseBtnCustom" 
            Content="✕" 
            shell:WindowChrome.IsHitTestVisibleInChrome="True"
            Click="CloseBtn_Click" 
            Style="{StaticResource ControlButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
